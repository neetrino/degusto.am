/**
 * Admin sidebar/mobile hub entry and /admin/messages routes.
 * Contact data and server actions stay in place — flip to `true` to restore admin UI.
 */
export const SHOW_ADMIN_MESSAGES_UI = false;
