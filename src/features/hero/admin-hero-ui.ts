/**
 * Admin CMS entry for hero slides (sidebar, mobile hub, /admin/hero).
 * Feature code and storefront hero stay in place — flip to `true` to restore admin UI.
 */
export const SHOW_ADMIN_HERO_UI = false;
