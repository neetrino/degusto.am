/**
 * Admin CMS entry for blog posts (sidebar, mobile hub, /admin/blog).
 * Feature code and storefront blog stay in place — flip to `true` to restore admin UI.
 */
export const SHOW_ADMIN_BLOG_UI = false;
