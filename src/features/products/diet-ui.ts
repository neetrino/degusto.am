/**
 * Front-facing hot / veggie UI (filter switcher, product badges, admin toggles).
 * Data columns and API fields stay in place — flip to `true` to restore the UI.
 */
export const SHOW_DIET_UI = false;
