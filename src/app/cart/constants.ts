/**
 * LocalStorage key for guest cart
 */
export const CART_KEY = 'shop_cart_guest';

/** `drawer`: cart drawer panel; `page`: full-width card rows (no `/cart` route — drawer-only cart). */
export type CartListAppearance = 'page' | 'drawer';
