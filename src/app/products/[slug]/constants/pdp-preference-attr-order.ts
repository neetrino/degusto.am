/**
 * Column order for PDP preference attributes (food options). Unknown keys follow in alphabetical order.
 */
export const PDP_PREFERENCE_ATTR_ORDER: readonly string[] = ['sauce', 'garlic'];

/**
 * Food prefs inside the PDP card (checkbox list).
 */
export const PDP_CARD_PREFERENCE_ORDER: readonly string[] = ['garlic', 'sauce'];
