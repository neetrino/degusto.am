import { ProductPageShell } from './ProductPageShell';

/** Instant skeleton on client navigations to PDP (before Suspense resolves). */
export default function ProductPageLoading() {
  return <ProductPageShell />;
}
