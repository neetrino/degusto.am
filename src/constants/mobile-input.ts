/** Tailwind `lg` breakpoint — mobile input zoom guard applies below this width. */
export const MOBILE_VIEWPORT_MEDIA_QUERY = '(max-width: 1023px)';

/** 16px — avoids iOS Safari zoom when an input is focused. */
export const MOBILE_INPUT_TEXT_CLASS = 'text-base leading-6';
