/**
 * Fullscreen PDP product image zoom sits above storefront headers
 * ({@link MOBILE_FIGMA_HEADER_STACKING_CLASS} `z-[100]`, desktop `z-50`)
 * and below the cart drawer (`z-[190]`).
 */
export const PDP_PRODUCT_IMAGE_ZOOM_STACKING_CLASS = 'z-[150]';
