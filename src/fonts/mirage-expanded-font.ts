import localFont from 'next/font/local';

/** Mirage Expanded — Armenian/Latin; see `Mirage-Expanded-LICENSE.txt` in this folder. */
export const mirageExpandedFont = localFont({
  src: './Mirage-Expanded.otf',
  display: 'swap',
});
